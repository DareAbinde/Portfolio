[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  framework = "#custom"
  command = "npm run dev:vite"
  targetPort = 5174
  port = 5173
  autoLaunch = false
